import PropTypes from 'prop-types';
const Blog = ({blog}) => {
      console.leg(blog)
      return (
            <div>
                  
            </div>
      );
};

Blog.propTypes = {
      blog: PropTypes.object.isRequired
}

export default Blog;